"""Open Listening through the authoritative single-origin ZaagGenZ runtime."""
from zaaggenz_runtime.__main__ import run

def main():run(default_workspace='listening',default_open=False)
if __name__=='__main__':main()
