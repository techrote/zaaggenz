# ZG-024d staged deterministic search protocol v1 — superseded before outcome inspection

This initial PR #195 draft is retained only as research-history evidence. Acceptance review found it incomplete before any v1 calibration/confirmatory outcome was inspected: it did not implement the issue #92 structural/spectral/texture stage split, Pareto alternative retention, verified-prefix resume, or required fresh fixture programme. **No v1 CI outcome is valid for method selection.** The pre-outcome amendment and complete design freeze are `ZG024_STAGED_PROTOCOL_V2.md`.

The obsolete draft had proposed a simpler global-Halton → generic coordinate-refinement experiment with three global fractions (1/3, 1/2, 2/3), seeds 41/211/2027, a single nonlinear calibration target, and two audit targets. Those targets and thresholds are not reused as v2 confirmation evidence.

This file intentionally contains no result or winner claim. It exists so the methodological correction is visible rather than silently rewriting the initial plan.
