"""Open Vocal gestures through the authoritative single-origin ZaagGenZ runtime."""
from zaaggenz_runtime.__main__ import run

def main():run(default_workspace='vocal',default_open=False)
if __name__=='__main__':main()
